{
  "name": "recall-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "migrate:local": "wrangler d1 migrations apply recall --local",
    "migrate:remote": "wrangler d1 migrations apply recall --remote",
    "hash-password": "node scripts/hash-password.mjs"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.4.3",
    "hono": "^4.7.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260708.1",
    "typescript": "^5.7.0",
    "wrangler": "^4.0.0"
  }
}
